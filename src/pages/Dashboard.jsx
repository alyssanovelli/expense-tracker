function Dashboard() {
    return (
        <div>
            <div className="dashboard-header">
                <h1>Dashboard</h1>
                <p>Welcome, {user.name}!</p>
            </div>
        </div>
    );
}